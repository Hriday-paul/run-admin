import OrdersContainer from '@/components/(adminDashboard)/Orders/OrdersContainer';
import React from 'react';

const page = () => {
    return (
        <div>
            <OrdersContainer />
        </div>
    );
};

export default page;
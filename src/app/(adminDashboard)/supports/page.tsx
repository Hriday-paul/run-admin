import SupportContainer from '@/components/(adminDashboard)/support/SupportContainer';
import React from 'react';

const page = () => {
    return (
        <div>
            <SupportContainer />
        </div>
    );
};

export default page;